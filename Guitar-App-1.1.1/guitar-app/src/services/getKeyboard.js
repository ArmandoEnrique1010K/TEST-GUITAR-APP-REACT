import { keyboard } from "../data/keyboard";
import { neck } from "../data/neck";

export const getKeyboard = () => {
  return keyboard;
};

// export const setKeysInNeck = ([one, two, three, four, five, six, start]) => {
//   // ASIGNAR CADA ELEMENTO DEL ARREGLO

//   return {};
// };
